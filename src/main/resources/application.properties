# DB (adjust as needed)
spring.datasource.url=jdbc:mysql://localhost:3306/unitech?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=root

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Server
server.port=8080
